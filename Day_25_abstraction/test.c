#include<stdio.h>
void main()
{
    char grade;
    float avg;

    printf("Enter avg: ");
    scanf("%f",&avg);

    grade = (avg >= 80)
                ? 'A'
                : (avg >= 60)
                ? 'B'
                : (avg >=

}